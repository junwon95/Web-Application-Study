#Gmail SMTP
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: yourmail@gmail.com
    password: yourpassword(don't push!)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
